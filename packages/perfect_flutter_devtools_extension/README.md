# perfect_flutter_devtools_extension

Internal Flutter web app. The DevTools panel UI for the `perfect_flutter`
package. Built artifacts are copied into
`packages/perfect_flutter/extension/devtools/build/` by
`melos run build:extension`.

Not published to pub.dev.
