/// perfect_flutter — DevTools-based pixel-perfect overlay for Flutter.
///
/// This package has no public API. Add it to your `dev_dependencies` and the
/// "Perfect Flutter" tab will appear in DevTools when your app is running in
/// debug mode. All overlay rendering is injected at runtime through the VM
/// service — there is nothing to import.
library;
